{
  "name": "@ft/game-service",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:watch": "tsc -p tsconfig.json --noEmit --watch --preserveWatchOutput",
    "test": "vitest",
    "test:run": "vitest run",
    "simulate": "tsx src/cli/game_simulate.ts"
  },
  "dependencies": {
    "@ft/game-ws-protocol": "*",
    "dotenv": "^17.2.4",
    "openskill": "^4.1.0",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  }
}