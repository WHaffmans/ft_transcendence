http:
  routers:
    frontend-service:
      rule: "(Host(`{{ env "DOMAIN" }}`) || Host(`www.{{ env "DOMAIN" }}`) || Host(`localhost`))"
      service: frontend-service
      priority: 1
      entryPoints:
        - web
        - websecure

    auth-service:
      rule: "(Host(`{{ env "DOMAIN" }}`) || Host(`www.{{ env "DOMAIN" }}`) || Host(`localhost`)) && PathPrefix(`/auth`)"
      service: backend-service
      priority: 10
      entryPoints:
        - web
        - websecure
      middlewares:
        - backend-service-strip

    backend-service:
      rule: "(Host(`{{ env "DOMAIN" }}`) || Host(`www.{{ env "DOMAIN" }}`) || Host(`localhost`)) && PathPrefix(`/api`)"
      service: backend-service
      priority: 10
      entryPoints:
        - web
        - websecure


    game-ws:
      rule: '(Host(`{{ env "DOMAIN" }}`) || Host(`www.{{ env "DOMAIN" }}`) || Host(`localhost`)) && PathPrefix(`/ws`)'
      service: game-service
      priority: 50
      entryPoints: [websecure, web]
      tls: {}

  services:
    frontend-service:
      loadBalancer:
        servers:
          - url: "http://frontend-service:5173"

    backend-service:
      loadBalancer:
        servers:
          - url: "http://backend-service:4000"

    game-service:
      loadBalancer:
        servers:
          - url: "http://game-service:3003"

  middlewares:
    backend-service-strip:
      stripPrefix:
        prefixes:
          - "/auth"

    auth-forward:
      forwardAuth:
        address: "http://backend-service:4000/api/verify"
        trustForwardHeader: true
        authResponseHeaders:
          - "X-User-Id"
