# Game WebSocket Protocol

## About

This package defines the **WebSocket message protocol** used for real-time communication between the **frontend (client)** and the **game-service (server)**.

A common package includes the envelope surrounding each message. Communication is defined between client and server.

---
<br/>


## Installation

Install Zod

```bash
cd packages/protocol
npm i zod
```
